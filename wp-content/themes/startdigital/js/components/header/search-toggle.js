import ToggleStateManager from './menu-state-manager'

// This function is now handled by the ToggleStateManager
// Keep this export for compatibility, but it's essentially a no-op
export default function searchToggle() {
	// The ToggleStateManager handles all search toggle functionality
	// This function is kept for backward compatibility
}
